# Encode a PKCE code verifier into a state string for the auth request

Encode a PKCE code verifier into a state string for the auth request

## Usage

``` r
pkce_state_encode(verifier)
```

## Arguments

- verifier:

  a PKCE code verifier string
